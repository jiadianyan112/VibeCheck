目标：跑通当前 Learning 主流程：P10 URL-check/create → 四步本地填写 → 完整 canonical PATCH → 封面上传与引用 → 把封面引用二次 PATCH 回 snapshot → 公开地址证据 → preview/submit → `pending_review` 回执。审核决定、首次发布和 ProjectUpdate 回流留后续。
顺序：空草稿实测 → 中间步骤仅本地缓存 → 最终字段与 catalog 约束校验 → 双 PATCH 资产/证据编排 → 服务端 preview/submit → 冲突与幂等重放 → 状态化黄金路径 → 全门禁/提交/CI。
基线：2026-08-24，HEAD 78aefc6；contracts:check=85 paths/95 ops；相关 3 测试文件 21 passed；Playwright --list=72 tests/9 files；build:libraries 通过；@vibecheck/contracts 正式导入通过。
范围：生产路径不回退 Mock；本轮只验收从公开 URL 到 `pending_review` 的主流程，不把本地状态化测试冒充真实部署 E2E，也不扩展审核完成后的分支。
最大风险：真实 API/storageState/唯一测试 URL 缺失导致真实 E2E 无法执行（证据见 BLOCKED.md）；409 冲突分支的“服务端事实优先”取舍；payload_snapshot snake_case 映射遗漏。
取舍：服务端事实 > 数据安全 > 用户输入不丢 > 交付速度；409 不静默覆盖，410 停止编辑，401/403 保留输入。
验证：真实空草稿 UI 四步、双 canonical PATCH、服务端返回 ID/版本复用、上传回执、证据绑定、preview/submit、`pending_review`、PATCH 响应丢失后的精确重放、409 服务端字段权威刷新、422 分类与滞后回执保护均有聚焦测试。
状态：P10/P11、P12A 和 P12B 当前主流程均已完成本地实现与门禁；真实上传扫描、真实部署与真实 E2E 仍未完成，不以客户端测试冒充真实验收。
P12A 验收边界：Media 仅在服务端 ready+clean+exif_removed=true 时允许 cover reference；Evidence 严格复用服务端返回版本并保留 409/422/transport/protocol/http 错误。真实 API 部署、storageState、唯一测试 URL、AWS signed-upload/扫描与部署 flag 仍见 BLOCKED.md，未验收。
上一阶段交付记录：P10/P11 的全门禁、clean clone、push 与 quality CI 已以 cf0972c593bdb6963a17db05590746ce5addb579（run 32807812891）完成。P12A 阶段只交付前置客户端与网关；P12B 的页面、preview/submit 与状态化同源流程见下方门禁记录，真实上传扫描和真实 E2E 仍未完成。

P12B Task 1C2B 门禁记录（2026-08-28，HEAD 6e393ff6aaa2b06cc1330f0c48ba7f78217da96c）：聚焦前端 Vitest 7 files / 87 tests passed；`npm test -w @vibecheck/contracts` 135 passed、0 failed（4 suites）；`npm run contracts:check` 通过（85 paths / 95 operations）；`npm run lint` 通过（0 errors、16 warnings，均为 `react-refresh/only-export-components`）；`npm run typecheck` 通过；`npm run build` 通过（Vite 188 modules transformed，HTML 1.46 kB、CSS 57.57 kB、JS 815.27 kB；仅有 chunk >500 kB 提示）。
本轮相关门禁通过后，canonical Learning snapshot、one cover + one URL evidence preparation、server preview/submit、`pending_review` receipt，以及 stateful same-origin submission golden-path integration 均可记录为本地门禁已完成；这表示客户端/契约与同源状态化测试证据成立，不等同真实部署验收。PostgreSQL fixture 未完成：本机无 `DATABASE_URL`，`npm run submission:submit:fixture:verify` exit 1，精确错误为 `Error: CONFIG_DATABASE_URL_REQUIRED`；不把该 fixture 标绿，CI 仍为权威。

P12B 主流程最终修正门禁（2026-08-28，已提交当前分支）：聚焦前端 7 files / 88 tests passed；contracts 135/135 passed；contracts check 85 paths / 95 operations；lint 0 errors / 16 既有 warnings；typecheck 通过；build 通过（188 modules，JS 815.94 kB，仅 chunk >500 kB 提示）。真实空草稿不再在第一步构造完整 snapshot；封面 reference ID 会以稳定 operation ID/observedAt 二次 PATCH，evidence 绑定其响应版本；编辑后不能绕过材料重跑直接预览，替换封面会先幂等删除旧 active cover；冲突刷新使用服务端已返回字段；fields-only PATCH 在类型和运行时双重禁止。PostgreSQL fixture 仍因本机缺少 `DATABASE_URL` 精确失败为 `CONFIG_DATABASE_URL_REQUIRED`，等待 pushed HEAD 的 CI 权威结果。

P01 Task 9 本地门禁记录（2026-08-29，base `925bccd962a6e004be306223505c59a2d7eb1a61`）：新增 `e2e/highfi-visual.spec.ts`，保留 `e2e/responsive.spec.ts` 的 360/390/768 与 submission/comparison checks，并将桌面视口 1280 改为 1440；`/projects` 已核验同时存在于 responsive 与 axe route lists，`e2e/accessibility.spec.ts` 无需无意义改动。视觉 spec 先以缺失快照红灯（3 failed / 1 passed）证明断言生效；滚动触发一次性 Reveal 后生成并稳定重跑三张 full-page baseline：`p01-mobile-desktop-chromium-win32.png` 390x10203 / 437627 bytes，`p01-tablet-desktop-chromium-win32.png` 768x7489 / 449561 bytes，`p01-desktop-desktop-chromium-win32.png` 1440x6904 / 519062 bytes。三张图逐张检查：文字无裁剪，项目操作未消失，媒体主体未被完全裁掉，横向作品带边界可见，未发现页面级溢出；默认视觉 4/4（含 reduced-motion）无快照差异通过。
浏览器 gate 记录：使用既有构建产物运行 desktop 组合 17 项，13 passed / 4 failed；mobile responsive 4 项，2 passed / 2 failed。失败均为 base 中既有 T54/T55 登录路径与当前真实邮箱 OTP UI 不匹配（页面没有已移除的“使用米娅账号/使用林舟账号”按钮），并且匿名点击作品“收藏”触发既有 `AuthGate` Router context TypeError 后显示错误面板；未删除检查、未 skip、未改产品源代码。Playwright 自带 webServer 另一次启动在 60 秒窗口超时（独立 `npm run build` 随后成功），无手工 preview 遗留。
全量本地 gate：`npm run lint` 0 errors / 17 Fast Refresh warnings；`npm run typecheck` PASS；`npm test` 68 files / 384 tests passed；`npm run build` PASS（Vite 203 modules，HTML 1.46 kB，CSS 84.59 kB / gzip 13.78 kB，JS 822.24 kB / gzip 239.12 kB，仅保留既有 chunk >500 kB 提示）；`npm run frontend:budget` PASS（JS gzip 238665 / 251435，CSS gzip 13674 / 17749）；`git diff --check` PASS。剩余范围：当前分支仍需 Sol review、提交后的 push/quality CI，以及真实部署/API、authenticated storageState、唯一公开测试 URL、AWS signed-upload/scan；这些不在本地 P01 visual gate 内。P01 visual completion is not real-deployment E2E。
P01 Task 9 correction round 1（2026-08-29，已提交 commit `7c5c80d3c402fa2354fd971551bf564c13fd8229`）：修复匿名收藏触发 `AuthGate` Router context TypeError 的根因，将 `AuthGateProvider` 放入 frontstage route 的 Router context；新增 `src/app/router.test.tsx` 回归测试，并将 T54/T55 登录路径迁移到当前真实邮箱 OTP UI。新增 E2E 仅通过浏览器网络层返回 auth/submission DTO，仍真实填写邮箱与 6 位 OTP，不注入 localStorage、不使用产品 test hook；公开 auth axe route 已统一为 `/auth?return_to=%2Fprojects`。门禁已重跑并全部通过：desktop highfi + responsive + accessibility 17/17；mobile responsive 4/4；focused router 14/14；全量 `npm test` 68 files / 385 tests；lint 0 errors / 17 既有 warnings；typecheck/build/budget/diff-check 均 PASS（JS gzip 238666/251435，CSS gzip 13674/17749）。真实 API/deployment、authenticated storageState、唯一公开测试 URL、AWS signed-upload/scan 仍不在本地门禁范围。P01 visual completion is not real-deployment E2E。
P01 Task 9 correction round 2（2026-08-30，已提交 commit `d3abcbc440e3f47820e5ea06fc9d989f3e8cabe9`）：视觉基线在 `/projects` 进入后通过真实 UI 点击“清空”并用确认对话框的真实聚焦控件确认，消除默认比较栏对“个人主页与作品集”标题的遮挡；390/768/1440 三张快照已更新并逐张检查，无标题或正文遮挡。键盘门禁已从旧的 `from`/原型账号按钮迁移为 `return_to` 邮箱 OTP，保持 focus、键盘输入和 Enter 提交；提交 API mock 改为 `installMockAuth(page, { submission: true })` 显式 opt-in，公开/登录 axe 用例不安装提交路由。门禁结果：desktop highfi + responsive + accessibility 17/17；mobile responsive 4/4；keyboard desktop 3 passed / 1 skipped（移动专属用例按设计跳过）；focused router 14/14；全量 `npm test` 68 files / 385 tests；lint 0 errors / 17 既有 warnings；typecheck/build/budget/diff-check 均 PASS（Vite 203 modules，JS gzip 238666/251435，CSS gzip 13674/17749）。4173 preview 已停止并确认无监听；未 push。P01 visual completion is not real-deployment E2E。
