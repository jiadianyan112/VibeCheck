import { PageFrame } from '../components'

export function WorkspacePage() {
  return (
    <PageFrame
      title="VibeCheck 原型工程"
      description="应用结构已建立，具体页面将在路由骨架任务中接入。"
    >
      <section aria-labelledby="workspace-status">
        <h2 id="workspace-status">当前状态</h2>
        <p>尚未实现业务页面。</p>
      </section>
    </PageFrame>
  )
}
