# T52 四条核心用户流程测试报告

## 基线断点记录

| 流程 | 已走通部分 | 集成断点 | 最小修复 |
| --- | --- | --- | --- |
| 广场 → 详情 → 收藏／比较 | 广场、详情、登录弹窗、收藏和比较状态均已实现 | 从详情返回列表依赖浏览器默认滚动恢复，没有应用级保障；详情数据覆盖变化会再次记录浏览事件 | 增加按历史记录键保存的滚动恢复；同一详情挂载期间只记录一次浏览 |
| 搜索 → 意图 → 分析 → 比较 → 行动 | 查询和意图由 URL 保存，比较与私密行动已实现 | 从分析页加入比较时，比较会话仍沿用旧的 `sourcePath`，返回可能落到错误列表 | 加入比较时同步当前完整路径到活动会话 |
| 发布 → 查重 → 新建／身份验证 | URL 检查、重复详情、返回发布和身份验证分支已实现 | 发布入口和编辑页的登录链接固定返回 `/submit`，会丢失场景、待恢复 URL、草稿与步骤 | 两个登录入口均保留当前完整路径和查询参数 |
| 个人中心 → 更新 → 时间线／动态 | 作者权限、更新、详情事件、动态和通知已共用状态 | 详情和动态对追加事件缺少统一按 ID 去重；成功后仍可重复提交且个人中心继续显示未完成草稿 | 详情与动态按稳定事件 ID 去重；成功后锁定提交并清理对应更新草稿 |

首次独立浏览器回归还发现两个运行时断点：异步列表尚未撑开页面时滚动恢复过早，以及右下角操作提示遮挡固定比较栏。滚动恢复现会在短窗口内等待列表完成渲染，操作提示移到页头下方。

## 修复后验收

| 流程 | 独立会话结果 | 关键断言 |
| --- | --- | --- |
| 广场 → 详情 → 收藏／比较 | 通过 | 登录弹层恢复收藏；比较状态同步；历史返回保留列表滚动 |
| 搜索 → 意图 → 分析 → 比较 → 行动 | 通过 | 查询与意图参数保留；比较来源为完整分析 URL；登录后私密行动恢复 |
| 发布 → 查重 → 新建／身份验证 | 通过 | 登录回跳保留参数；默认地址进入新建；重复地址不新建并可转身份验证 |
| 个人中心 → 更新 → 时间线／动态 | 通过 | 一次更新只生成一个稳定事件；详情、动态和通知共用事件 ID；完成草稿清理 |

## 自动化结果

- `npm test -- --run`：55 个测试文件、252 项测试全部通过。
- `npm run test:e2e`：5 项通过、5 项按设备职责预期跳过；其中 T52 四条桌面流程全部通过，移动比较烟雾测试通过。
- `npm run typecheck`：通过。
- `npm run lint`：0 错误，保留 15 条既有 Fast Refresh 结构警告。
- `npm run build`：通过；产物主脚本 655.63 kB，保留 Vite 单包超过 500 kB 的非阻塞提示。
- `git diff --check`：通过，仅显示 Windows 行尾转换提示。

## 浏览器实测补充

- `/search?q=PDF&status=normal` 进入详情再返回后，URL 筛选与滚动位置均恢复。
- 比较会话的“来源页”指向加入比较时的完整筛选 URL。
- `/submit?scenario=duplicate_project` 登录后仍回到同一场景，查重详情返回后保留规范化 URL。
- 作者更新事件在详情与 `/activity#事件ID` 各出现一次，锚点可定位。

## 结论

T52 验收通过，无阻塞遗留。构建体积和 Fast Refresh 警告在 T56 质量报告中继续记录。
