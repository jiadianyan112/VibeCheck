# T14 实现 ProjectCard 三种变体

## 实施摘要

建立 compact、standard、event 三种作品卡片。组件只消费显式传入的 Project、Evidence、LifecycleEvent 与交互状态，不在内部读取服务或全局状态。

## 改动文件

- `src/components/domain/ProjectCard.tsx`：三种变体与受控交互。
- `src/components/domain/ProjectCard.test.tsx`：变体、链接、来源与回调测试。
- `src/styles/global.css`：卡片布局和窄屏规则。

## 运行命令

- `npm run typecheck`
- `npm run test`
- `npm run build`

## 验收结果

- [x] compact、standard、event 变体均可独立使用。
- [x] 当前状态与历史事件在不同变体中明确分离。
- [x] 未知字段保留原因，证据入口显示数量。
- [x] 收藏、关注与比较均以受控 props 和回调表达。
- [x] 类型检查、测试和构建通过。

## 页面入口

供 T17 及后续前台页面组合使用。

## 遗留问题

类型检查、48 项单元测试和生产构建均通过。

## 下一任务依赖

T15 将为需要登录的卡片操作提供门控和动作恢复。

## 提交信息

`feat(T14): add project card variants`
