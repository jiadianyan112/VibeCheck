# T42 补齐发布与更新异常及草稿恢复

## 实施摘要

统一发布 URL、发布审核、作者身份审核和作品更新四条链路的错误呈现、稳定 code、原位重试与本地恢复。新增作品更新持久化草稿和权限失效场景；身份材料在请求发出前保存；发布 URL 跨页恢复。重复重试使用稳定审核/更新标识，不重复创建草稿、审核单或生命周期事件。

## 前置条件检查

- [x] T36—T41 的发布、审核、身份验证和更新流程均已完成。
- [x] 已核对任务书 T42 全部异常类型与阶段退出要求。
- [x] 现有错误运行时已提供 network、timeout、server、validation 等统一结构。

## 改动文件

- `src/pages/SubmitEntryPage.tsx`：URL 输入持久化、稳定错误 code 和原位重试。
- `src/pages/SubmissionReviewPage.tsx`：审核服务失败后按当前状态原位重试，提交版本不变。
- `src/pages/AuthorVerificationPage.tsx`：材料请求前保存，区分加载错误与审核错误，失败后恢复和重试。
- `src/pages/ProjectUpdatePage.tsx`：持久化更新草稿、服务调用、网络/服务错误恢复与权限失效引导。
- `src/services/projectUpdateService.ts`：更新服务失败模拟和 `VC_UPDATE_PERMISSION_EXPIRED`。
- `src/state/*`、`src/types/domain.ts`：发布入口值和项目更新草稿持久化。
- `src/services/runtime.ts`：增加 `permission_expired` 固定场景。
- `docs/testing-scenarios.md`：全部异常场景的打开方式、预期 code 和恢复路径。
- 四条页面测试：跨页恢复、材料保留、提交版本保留、权限失效和重试幂等。

## 运行命令

- `npm run typecheck`
- `npm run lint -- --quiet`
- `npm test -- --run`
- `npm run build`
- `git diff --check`

## 验收结果

- [x] 网络中断、请求超时和服务错误均显示稳定 code，且不清空当前输入。
- [x] 自动提取部分失败保留已提取字段并允许失败字段手填。
- [x] 外链风险阻止继续，但待检查地址跨页保留。
- [x] 发布审核服务异常保留同一 `submittedFields`、`submittedAt` 和审核单。
- [x] 身份审核失败前已保存私有材料、方式和摘要，跨页恢复后可继续提交。
- [x] 更新网络/服务失败保留类型、值、来源、影响范围与资产字段。
- [x] 权限中途失效返回 `VC_UPDATE_PERMISSION_EXPIRED`，不写事件并引导重新验证。
- [x] 重试使用稳定 ID；测试确认更新记录和生命周期事件各只有一条。
- [x] 需修改审核意见继续按字段链接定位；本地校验错误在对应表单旁显示。
- [x] `docs/testing-scenarios.md` 提供所有场景的可复现打开方式。
- [x] typecheck、lint、177 项测试、生产构建和差异格式检查通过。

## 页面入口

- 完整场景表：`docs/testing-scenarios.md`
- 通用失败参数：`scenario=network_error|service_error|timeout`
- 权限失效：`/project/project-speakmirror/update?type=version&scenario=permission_expired`

## 遗留问题

无。生产构建仍有 Vite 的单包超过 500 kB 提示，不影响低保真原型功能或本阶段验收；最终性能检查可在 T55 统一处理。

## 下一任务依赖

阶段 7 已满足退出检查。T43 可依赖持久化用户资产、审核记录、更新记录和通知，开始个人中心闭环。

## 提交信息

`feat(T42): add resilient author workflow recovery`
