# T56 补齐自动化测试与稳定性检查

## 实施摘要

完成十类领域逻辑覆盖审计，新增 U01–U06 六项独立 Playwright 验收，复核 T52 四条主流程，并关闭自动化浏览器模拟等待与无价值随机 ID。完整质量命令全部通过。

## 前置条件检查

- [x] T52 四条主流程已贯通。
- [x] T53 固定场景矩阵已落地。
- [x] T54 响应式关键路径已通过。
- [x] T55 axe、键盘和焦点审计已通过。

## 主要改动

- `e2e/prototype-tasks.spec.ts`：逐项实现 U01–U06。
- `src/services/runtime.ts`：自动化浏览器不等待模拟延迟，业务场景保持不变。
- `src/components/feedback/Toast.tsx`：使用确定性递增 ID。
- `playwright.config.ts`：preview 前自动执行 build；失败保留截图和 trace。
- `react-router-dom` 固定为 7.18.2，并完成安全公告适用性评估。
- `docs/acceptance/T56-quality-report.md`：覆盖映射、耗时、失败修复和剩余风险。

## 验收结果

- [x] 搜索匹配、意图解析、比较差异、比较上限、持久化、查重、草稿、权限、事件和状态机均有领域断言。
- [x] U01–U06 六项原型任务全部通过。
- [x] 四条主流程全部通过。
- [x] 固定测试数据，无随机服务延迟或随机 Toast ID。
- [x] Playwright 可重复运行，失败截图、trace 与 HTML 报告可用。
- [x] 57 个测试文件、258 项单元测试全部通过。
- [x] 完整 Playwright 43 项通过、17 项按设备职责预期跳过。
- [x] typecheck、lint、unit、e2e、build 和 `git diff --check` 全部通过。

## 运行命令与耗时

- `npm run typecheck`：9.68 秒。
- `npm run lint -- --max-warnings=100`：6.52 秒，0 错误／15 警告。
- `npm test -- --run`：47.33 秒，57/57 文件、258/258 测试通过。
- `npm run build`：12.21 秒，生产主脚本 659.02 kB。
- `npx playwright test --reporter=list`：35.92 秒，43 通过／17 预期跳过。
- `git diff --check`：通过。

## 失败修复与剩余风险

详见 `docs/acceptance/T56-quality-report.md`。没有通过跳过业务逻辑、修改固定结果或删除断言来换取测试通过。

## 下一任务依赖

T57 只进行 README、需求映射、最终验收记录、死代码／日志／依赖和交付产物清理，不再增加功能。

## 提交信息

- Commit：本次提交。
- 建议消息：`test(T56): complete regression and stability checks`
