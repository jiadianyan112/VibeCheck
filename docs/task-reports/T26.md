# T26 实现结果不足、无精确结果与解析异常

## 实施摘要

为高意图流程补齐三类可复现出口：1—2 个精确结果与放宽结果分区；0 个精确结果时显示相邻分类并允许保存查询；解析超时或失败时保留原文，提供关键词结果和手工标签。

## 验收结果

- [x] 1—2 个精确结果始终保留，放宽作品单独标注且说明原因。
- [x] 0 个精确结果明确说明不等于需求不存在，并展示相邻分类。
- [x] 解析超时／失败后可进入关键词结果或手工构建标签。
- [x] 精确、放宽和相邻分类使用独立视觉与语义标签。
- [x] 提供修改条件、清除筛选、保存查询和返回作品广场出口。
- [x] 固定 URL 与全局场景开关可复现稀疏、零结果和超时状态。
- [x] 类型检查、89 项测试和生产构建通过。

## 测试入口

- 稀疏结果：`/discover/result?idea=PDF练习&target=university_students&scenario=question_generation&input=pdf&practice=single_choice&practice=short_answer&output=questions&output=practice_set`
- 零精确结果：`/discover/result?idea=教师日练&target=teachers&scenario=daily_practice&input=video&practice=dictation&output=flashcards`
- 解析异常：在“原型调试”中将模拟场景切换为 `timeout` 或 `parse_failure`，再进入 `/discover?idea=一个口语练习想法`。

## 遗留问题

无。

## 下一任务依赖

T27 实现作品详情顶部展示区。

## 提交信息

`feat(T26): add discovery fallback states`
