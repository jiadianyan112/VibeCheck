# T52 集成四条核心用户流程

## 实施摘要

按任务书逐条走通广场、搜索发现、发布和作者更新四条主流程。修复历史返回滚动、比较来源路径、发布登录回跳、重复浏览/生命周期事件、成功后重复提交、已完成更新草稿残留，以及提示条遮挡固定比较栏。

## 前置条件检查

- [x] T17—T51 主功能已完成并提交。
- [x] 先在 `docs/acceptance/T52-flow-test-report.md` 记录基线断点。
- [x] 所有修复均为页面、状态和跨页集成层的最小修改，没有重写既有页面。

## 主要改动

- `src/components/RouteScrollManager.tsx`：按历史记录键保存滚动，等待异步列表渲染后恢复，并支持异步锚点。
- `src/features/comparison/ComparisonBar.tsx`、`src/state/reducer.ts`：加入比较时同步完整来源路径。
- `src/pages/SubmitEntryPage.tsx`、`src/pages/SubmitFormPage.tsx`：登录回跳保留场景、URL、草稿与步骤查询参数。
- `src/pages/ProjectDetailPage.tsx`、`src/pages/ActivityPage.tsx`：浏览事件只记一次，生命周期事件按稳定 ID 去重。
- `src/pages/ProjectUpdatePage.tsx`、`src/state/reducer.ts`：成功后锁定本次提交并清除对应更新草稿。
- `src/styles/global.css`：操作提示移到页头下方，避免遮挡固定比较操作栏。
- `e2e/core-flows.spec.ts`：四条独立浏览器会话流程测试。

## 验收结果

- [x] 四条核心流程均可在独立浏览器会话完成。
- [x] 返回保留列表 URL 筛选与滚动位置。
- [x] 登录回跳和待执行行动恢复有效。
- [x] 比较来源、详情、动态、通知和个人中心使用一致状态。
- [x] 重复提交、重复事件和错误返回目标已修复。
- [x] 55 个测试文件、252 项单元测试全部通过。
- [x] Playwright 5 项通过、5 项按桌面/移动职责预期跳过。
- [x] typecheck、lint、生产构建和差异检查通过。

## 运行命令

- `npm run typecheck`
- `npm run lint`
- `npm test -- --run`
- `npm run test:e2e`
- `npm run build`
- `git diff --check`

## 遗留问题

无阻塞问题。生产构建仍提示主脚本超过 500 kB；lint 仍有 15 条既有 Fast Refresh 警告，均留待 T56 统一记录。

## 下一任务依赖

T53 可基于已贯通的流程建立统一场景切换器与异常矩阵，不再使用散落的临时开关。

## 提交信息

- Commit：本次提交。
- 建议消息：`feat(T52): integrate core user flows`

