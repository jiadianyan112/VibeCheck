# T50 实现审核、合并、身份与状态操作

## 实施摘要

实现发布通过、退回、拒绝、争议、限制展示、重复合并、身份审核和状态复核。所有动作要求原因与确认，高风险操作限制为管理员，并写入幂等工作流日志。

## 前置条件检查

- [x] T39、T40、T47-T49 已完成。
- [x] 前台、个人中心、通知和生命周期事件可接收后台结果。

## 改动文件

- `src/features/admin/workflows.ts`：审核、合并、身份与状态工作流。
- `src/pages/AdminReviewsPage.tsx`、`AdminDuplicatesPage.tsx`、`AdminAuthorVerificationPage.tsx`、`AdminStatusMonitorPage.tsx`：四个后台工作台。
- `src/state/reducer.ts`：幂等日志、稳定 ID 映射和下游引用更新。
- `src/pages/ProjectDetailPage.tsx`：限制展示和旧 ID 映射提示。

## 运行命令

- `npm run typecheck`
- `npm run lint`
- `npm test -- --run`
- `npm run build`

## 验收结果

- [x] 发布和身份审核同步个人中心与通知。
- [x] 合并保留主档稳定 ID、副档归档和历史。
- [x] 私有身份材料不进入公开日志。
- [x] 首次 URL 异常只进入待复查。
- [x] 53 个测试文件、244 项测试在任务完成时通过。

## 页面入口

- 路由：`/admin/reviews`、`/admin/duplicates`、`/admin/author-verification`、`/admin/status-monitor`
- 测试角色：平台编辑、原型管理员。

## 遗留问题

无。

## 下一任务依赖

T51 可在统一工作流上补齐异常恢复与一致性检查。

## 提交信息

- Commit：`f30a724`

