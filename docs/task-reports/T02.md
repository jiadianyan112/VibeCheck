# T02 建立代码目录与应用入口

## 实施摘要

建立任务书规定的源代码目录、应用入口、路由、全局 Provider、共享类型与样式入口，并记录目录职责和依赖方向；未实现业务页面或领域状态。

## 改动文件

- `src/app/*`：App、路由、Provider 和公共出口。
- `src/components`、`features`、`pages`、`mocks`、`services`、`state`、`types`、`utils`、`styles`、`tests`：最小真实职责文件。
- `src/main.tsx`：切换到应用和全局样式入口。
- `docs/decisions/architecture.md`：架构决策。

## 运行命令

- `npm run typecheck`
- `npm run test`
- `npm run build`

## 验收结果

- [x] 应用继续可启动并通过质量命令。
- [x] 目录职责已写入架构决策。
- [x] 没有以临时占位文件维持空代码目录。
- [x] 未实现具体页面和业务状态。

## 遗留问题

无。类型检查、1 项单元测试和生产构建全部通过。

## 下一任务依赖

T03 可在现有 `styles` 和 `components` 边界内建立低保真样式基线与组件展示页。

## 提交信息

`chore(T02): establish application architecture`
