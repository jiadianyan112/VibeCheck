# T22 实现统一搜索结果页

## 实施摘要

实现“搜作品／查同类”模式、查询头部、结果数量、四类筛选、排序、字段匹配原因、统一作品卡片和专用无结果状态。查询与所有控件状态均写入 URL。

## 验收结果

- [x] 作品名、PDF、口语评分等固定查询稳定返回。
- [x] 匹配原因来自作品字段，不生成笼统推荐结论。
- [x] 短词默认搜作品，长句默认查同类，切换不清空输入。
- [x] 刷新和返回可通过 URL 恢复查询、筛选与排序。
- [x] 无结果进入专用状态并提供后续路径。
- [x] 类型检查、测试和构建通过。

## 页面入口

`/search?q=PDF`

## 遗留问题

类型检查、69 项单元测试和生产构建均通过。

## 下一任务依赖

T23 实现确定性自然语言意图解析器。

## 提交信息

`feat(T22): implement unified search results`
